# scikit-learn must match the version that produced Height.joblib (1.6.1).
# Unpickling across versions raises InconsistentVersionWarning and is not guaranteed
# to load correctly, so this pin is deliberate — regenerate the model if you bump it.
scikit-learn==1.6.1
numpy==2.0.2
joblib==1.4.2

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
